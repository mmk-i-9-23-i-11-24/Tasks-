
# Лабораторная работа 4.  
# Разграничение прав доступа с помощью команд Linux

<https://www.webminal.org/> 

## Задание   
Необходимо настроить права доступа к директории, каталогам, файлам 
## Требования:  
- директория должна быть открыта для чтения и запись всем типам пользователей    
- каталоги 2 уровня должен быть открыт для чтения и записи остальным группам пользователей    
- один из каталогов 2 уровня должен быть открыт для чтения и записи только администратору  
- конечные файлы формата .md  должны быть открыты только для чтения всем типам пользователей  
- один из конечных файлов формата .md в каждом каталоге 2 уровня должн быть виден только администратору  


# Теоретические сведения

> **Права доступа означают разрешение на выполнение той или иной операции (чтение, записи, исполнения).** 
> 
> Когда пользователь входит в систему, его оболочка получает UID и GID (UID – идентификатор пользователя, GID - идентификатор группы), которые содержатся в его записи в файле паролей, и они наследуются всеми его дочерними процессами. Представляя любую комбинацию (UID, GID), можно составить полный список всех объектов (файлов, включая устройства ввода-вывода, которые представлены в виде специальных файлов и т.д.), к которым процесс может обратиться с указанием возможного типа доступа (чтение, запись, исполнение).
> 
> Два процесса с одинаковой комбинацией (UID, GID) будут иметь абсолютно одинаковый доступ к одинаковому набору объектов. Процессы с различающимися значениями (UID, GID) будут иметь доступ к разным наборам файлов, хотя, может быть, и со значительным перекрытием этих наборов.
> 
> **SUID (Set User ID)**
> 
> Атрибут исполняемого файла, позволяющий запустить его с правами владельца. В операционных системах Linux приложение запускается с правами пользователя, запустившего указанное приложение. Это обеспечивает дополнительную безопасность т.к. процесс с правами пользователя не сможет получить доступ на запись к важным системным файлам, например /etc/passwd, который принадлежит суперпользователю root. Если на исполняемый файл установлен бит suid, то при выполнении эта программа автоматически меняет «эффективный userID» на идентификатор того пользователя, который является владельцем этого файла. То есть, не зависимо от того - кто запускает эту программу, она при выполнении имеет права хозяина этого файла.
> 
> **SGID (Set Group ID)**
> 
> Аналогичен SUID, но относиться к группе. При этом, если для каталога установлен бит SGID, то создаваемые в нем объекты будут получать группу владельца каталога, а не пользователя.

### Пользователи и группы в Linux
В Linux каждый файл и каталог принадлежит определенному пользователю (владельцу) и группе. Файловая система поддерживает три уровня прав доступа:
- **Владелец** (owner) – пользователь, создавший файл.
- **Группа** (group) – группа, к которой принадлежит файл.
- **Остальные** (others) – все остальные пользователи.

### Права доступа
Права доступа к файлам в Linux выражаются в трех категориях:
- **r (read)** – чтение файла или просмотр содержимого каталога.
- **w (write)** – запись в файл или изменение каталога (создание/удаление файлов в нем).
- **x (execute)** – выполнение файла как программы или доступ к каталогу.

Просмотреть права доступа можно с помощью команды:
```sh
ls -l
```
Пример вывода:
```sh
-rwxr-xr--  1 user group  1024 Feb 6 12:34 example.sh
```
Структура прав доступа:
- Первое `-` указывает на тип (файл `-`, каталог `d`).
- `rwx` – права владельца.
- `r-x` – права группы.
- `r--` – права остальных пользователей.

### Описание команд

- `id` – печать идентификатора пользователя:
  ```sh
  id
  ```
  Выводит UID (идентификатор пользователя), GID (идентификатор группы) и дополнительные группы.

- `chgrp` – изменить группу файла:
  ```sh
  chgrp новая_группа файл
  ```
  Используется для смены группы владельца файла или каталога.

- `chown` – изменить владельца и группу файлов:
  ```sh
  chown новый_владелец файл
  ```
  ```sh
  chown новый_владелец:новая_группа файл
  ```
  Позволяет сменить владельца и группу файла.

- `chmod` – изменить права доступа к файлу:
  ```sh
  chmod 755 файл
  ```
  Устанавливает права доступа в числовом или символьном формате.

- `usermod` – изменение параметров учетной записи пользователя:
  ```sh
  usermod -aG группа пользователь
  ```
  Добавляет пользователя в указанную группу.

- `useradd` – создание нового пользователя:
  ```sh
  useradd новый_пользователь
  ```
  Создает учетную запись нового пользователя.

- `userdel` – удаление пользователя:
  ```sh
  userdel пользователь
  ```
  Удаляет учетную запись пользователя.

- `whoami` – определение текущего пользователя:
  ```sh
  whoami
  ```
  Показывает имя активного пользователя.

- `umask` – определение или установление маски прав доступа:
  ```sh
  umask
  ```
  ```sh
  umask 027
  ```
  Показывает или задает маску прав доступа для новых файлов.

- `sudo su` – получение прав суперпользователя:
  ```sh
  sudo su
  ```
  Позволяет войти в систему как суперпользователь.

- `groups` – определение групп, к которым принадлежит пользователь:
  ```sh
  groups
  ```
  Выводит список групп текущего пользователя.

## Консольные команды:

- id <печать идентификатора пользователя>  
- chgrp <изменить группу файла>  
- chown <изменить владельца и группу файлов>  
- chmod <изменить права доступа к файлу>  
- usermod <изменение параметров учетной записи пользователя>  
- useradd <создание нового пользователя>  
- userdel <удаление пользователя>  
- whoami <определение текущего пользователя>  
- umask <определение или установление маски прав доступа для вновь создаваемых файлов>  
- sudo su <получение прав суперпользователя>  
- groups <определение к каким группам принадлежит пользователь>  


## Контрольные вопросы
1. Какую команду использовать для изменения прав доступа к файлу?  
2. Каковы основные категории пользователей в системе Linux?  
3. Чем отличаются команды chown и chgrp?  
4. Что означает право x для каталога?  
5. Как можно сделать файл доступным только для чтения всем пользователям?  
6. Как проверить текущие права доступа к файлу?  
7. Какие уровни прав доступа существуют в Linux?  
8. Как посмотреть текущие права файла?    
9. Как изменить права доступа для владельца, группы и остальных пользователей?  
10. Как изменить владельца файла?  
11. Что такое `umask` и зачем он нужен?  


## После выполнения работы оформить отчет по лабораторной работе
в отчете должна содержаться следующая информация: 
- титульный лист с указанием предмета, фио и группы
- краткая теория (с описанием предметной области)
- цели и порядок выполнения работы
- скриншоты
- ответы на контрольные вопросы
- выводы
- список используемой литературы

при написании отчета не забывайте об общих правилах форматирования  
файл с отчетом назвать следующим именем: порядковый номер лабораторной работы_фамилия_тема работы (например: 4_Иванов_USE CASE)  
полученный отчет необходимо экспорторовать в PDF и залить на github (рекомендую выгрузить и doc/docx и pdf)  
